import Logo from "components/ui/Logo";

const Hero = () => {
	return (
		<section className="h-screen w-full bg-hero bg-center bg-no-repeat bg-cover overflow-hidden flex items-center justify-center">
			<Logo />
		</section>
	);
};

export default Hero;
